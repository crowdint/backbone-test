<section>
<header>
  <h1>{{ name }} </h1>
</header>
  <ul>
    {{#each treatments }}
    <li id={{ @key }}>
      <a href='#condition/{{../name}}/treatment/{{@key}}'>{{ display_name }}</a>
    </li>
    {{/each}}
  </ul>

<footer>
  <nav>
  {{#each breadcrumb }}
  <a href='{{this}}'>{{@key}}</a>
  {{/each}}
  </nav>
</footer>
</section>
